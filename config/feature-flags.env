# ContosoPay feature flags (committed, non-secret).
#
# DEPRECATED as the leak toggle. The GitOps source of truth for the planted
# memory-leak fault is now infra/leak.auto.tfvars (applied by CI on merge).
# This file is kept only for local reference and is not consumed by Terraform,
# the deploy scripts, or CI.
ENABLE_SLOW_LEAK=false
