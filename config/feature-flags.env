# ContosoPay feature flags (committed, non-secret).
#
# ENABLE_SLOW_LEAK is the planted-fault toggle for payment-service. Flipping it
# to "true" via scripts/trigger-incident.sh is shipped as a real git commit so
# the Azure SRE Agent can correlate the incident back to this change.
ENABLE_SLOW_LEAK=true
