{
  "name": "contosopay-payment-service",
  "version": "1.0.0",
  "private": true,
  "description": "ContosoPay payment-service (internal) — contains the feature-flagged demo memory leak.",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts"
  },
  "dependencies": {
    "@azure/monitor-opentelemetry": "^1.8.0",
    "@opentelemetry/api": "^1.9.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
